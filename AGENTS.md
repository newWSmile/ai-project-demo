# AI 应用学习项目宪法

本文件是仓库级强制规范，适用于本目录及所有子目录。任何新的 AI 对话、编码代理或开发者在修改项目前，都必须先阅读并遵守本文件。

## 一、语言与注释

1. 项目中的重要类、重要方法和关键执行步骤必须提供清晰的中文注释。
2. 必须注释的内容包括：
   - 对外接口、核心业务入口和配置 Bean；
   - 模型调用、请求组装、响应解析和协议转换；
   - Token、超时、重试、流式响应、线程中断和资源释放；
   - 异常转换、安全处理、兼容性处理及不直观的技术选择；
   - 一个方法内具有先后依赖关系的关键步骤。
3. 注释应说明“为什么这样做”“该步骤解决什么问题”或“有什么风险”，禁止为普通赋值、Getter、显而易见的语句添加无意义注释。
4. 面向学习者的默认系统提示词、示例提示词、校验提示和业务异常信息优先使用中文；协议固定字段、类名、方法名及第三方 API 字段保持其原始英文名称。
5. Spring AI 与原生 HTTP 等对比实验必须复用相同的系统提示词，避免实验条件不一致。

## 二、UTF-8 编码

1. 所有源代码、配置、Markdown、JSON、YAML、脚本和 `.http` 文件必须使用 UTF-8 编码。
2. Java/Maven 项目必须显式配置 `project.build.sourceEncoding` 和 `project.reporting.outputEncoding` 为 `UTF-8`。
3. JSON 请求应声明 `Content-Type: application/json; charset=UTF-8`。
4. SSE 响应必须确保服务端实际返回 `Content-Type: text/event-stream;charset=UTF-8`，不能只依赖客户端猜测字符集。
5. 新增或修改包含中文的文件后，应完成编译或严格 UTF-8 解码检查，防止出现本地正常、其他工具乱码的问题。

## 三、HTTP 测试记录

1. 每个可运行项目都必须维护 `httplog/<项目名>.http`。
2. 每个请求使用 `### 测试名称` 分隔，并用 `# 用途：...` 说明验证目标。
3. 常规成功调用默认不添加 IDEA HTTP Client 断言，保持请求记录简洁。
4. 只有在排查或回归已发生的异常、边界行为、编码问题、协议兼容问题时，才添加与该问题直接相关的针对性断言；禁止机械添加通用成功断言。
5. `.http` 文件中禁止出现真实 API Key、密码、Token 或其他敏感信息。

## 四、环境与密钥

1. 不修改本机系统级 Java 8 配置；需要 Java 21 的项目通过项目脚本或 IDE 项目配置单独指定。
2. API Key 必须通过环境变量或 IDE 私有启动配置注入，禁止硬编码、写入日志或提交到 Git。
3. 示例配置只使用安全占位值，并提供 `.env.example` 或等价说明。

## 五、变更完成标准

1. 代码能够使用项目指定的 JDK 编译。
2. 相关自动化测试通过；新增接口同步补充对应 `.http` 请求，异常场景按需增加针对性断言。
3. 重要方法和关键步骤具备中文注释，文件通过 UTF-8 检查。
4. 学习任务完成后同步更新任务清单和实验日志；未经真实验证的事项不得提前勾选。
