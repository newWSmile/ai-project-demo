---
description: AI 应用学习仓库的强制项目宪法
alwaysApply: true
---

# 项目宪法入口

执行任何代码分析或修改前，必须完整读取并遵守仓库根目录的 `AGENTS.md`。

核心要求：

- 重要类、重要方法和关键步骤必须有说明原因、目的或风险的中文注释。
- 所有源码、配置、文档和 HTTP 测试文件必须使用 UTF-8。
- 新增接口必须同步维护 `httplog/<项目名>.http`；常规调用不添加断言，异常场景才按需添加针对性断言。
- API Key 等敏感信息只能通过环境变量或私有启动配置注入。
